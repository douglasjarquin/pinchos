# Recipe: HTTP Health
# Category: Network
# Expected output: "200"
# Replace example.com with an endpoint you operate.

[item.http]
run = "curl -sS -o /dev/null -w '%{http_code}' --max-time 5 https://example.com"
interval = "5m"
timeout = "8s"
format = "HTTP {output}"
symbol = "heart"
