# Recipe: Local IP
# Category: Network
# Expected output: "192.168.1.42"
# This assumes the primary interface is en0.

[item.local_ip]
run = "ipconfig getifaddr en0 || printf offline"
interval = "1m"
format = "{output}"
symbol = "network"
