# Recipe: Battery
# Category: Power
# Expected output: "80%"
# On a desktop Mac this command may produce no value.

[item.battery]
run = "pmset -g batt | grep -Eo '[0-9]+%' | head -1"
interval = "1m"
format = "{output}"
symbol = "battery.75"
