# Recipe: Git Branch
# Category: Development
# Expected output: "main"
# Replace /path/to/repository with an absolute path.

[item.branch]
run = "git -C /path/to/repository branch --show-current"
interval = "1m"
format = "{output}"
symbol = "arrow.triangle.branch"
